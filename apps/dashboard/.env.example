# Dashboard env (copy to .env.local for local dev)
# Format: KEY=value (no spaces around =)

# API base URL - no trailing slash. REQUIRED on Vercel (no localhost fallback in production).
# Local dev: http://localhost:8000
# Vercel / production: your API URL, e.g. http://89.167.81.215:8000
NEXT_PUBLIC_API_BASE=http://localhost:8000
