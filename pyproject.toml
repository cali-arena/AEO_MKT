[project]
name = "ai-mkt"
version = "0.1.0"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["apps/api/tests", "tests", "eval"]
pythonpath = ["."]
addopts = "-q"
markers = [
    "requires_db: tests that require Postgres DATABASE_TEST_URL",
]
