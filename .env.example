# Backend env for VPS (copy to .env)
# Format: KEY=value (no spaces around =, no quotes unless value has spaces)

ENV=prod

# Postgres (used by docker-compose for postgres container + DATABASE_URL)
# DATABASE_URL must use service name "postgres" when running in Docker (not localhost)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=changeme
POSTGRES_DB=ai_mkt

# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
# In Docker: use host "postgres" (service name)
DATABASE_URL=postgresql://postgres:changeme@postgres:5432/ai_mkt

# Test DB (required for make test and DB tests). Must end with _test. Local Postgres only.
# Use DATABASE_TEST_URL (preferred) or DATABASE_URL_TEST.
DATABASE_TEST_URL=postgresql://postgres:postgres@localhost:5432/ai_mkt_test
DATABASE_URL_TEST=postgresql://postgres:postgres@localhost:5432/ai_mkt_test

# CORS: comma-separated origins, no trailing slash
# Include your Vercel frontend URL and localhost for dev
CORS_ALLOW_ORIGINS=https://your-app.vercel.app,http://localhost:3000,http://localhost:8501

# Embeddings: deterministic (hash-based, no network) | huggingface (real model, prod default)
EMBED_PROVIDER=huggingface

# Full-text search language: simple | english | ...
FTS_LANG=simple

# Optional: Redis (enable with docker compose --profile redis)
# REDIS_URL=redis://redis:6379/0

# Optional: Git SHA for eval runs
# GIT_SHA=abc1234
